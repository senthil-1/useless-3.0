import Link from 'next/link'; import { ArrowUpRight, FileWarning, Award, SearchCheck } from 'lucide-react'
const icons={incident:FileWarning,cert:Award,track:SearchCheck}
export default function ServiceCard({type,title,desc,href,accent}:{type:keyof typeof icons,title:string,desc:string,href:string,accent:string}){const I=icons[type];return <Link href={href} className="group card rounded-2xl p-6 hover:-translate-y-1 transition-transform block"><div className="flex justify-between"><div className={`w-11 h-11 rounded-xl grid place-items-center ${accent}`}><I size={20}/></div><ArrowUpRight className="text-slate-400 group-hover:text-[#6f1020]"/></div><h3 className="serif text-2xl mt-7">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{desc}</p><div className="mt-6 text-sm font-bold text-[#10243d]">Open service →</div></Link>}
